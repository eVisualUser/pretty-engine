#ifndef H_PRETTY_MATH
#define H_PRETTY_MATH

#include <glm/glm.hpp>

namespace PrettyEngine {
	static glm::vec3 AABBClosestPoint(glm::vec3 minA, glm::vec3 maxA, glm::vec3 minB, glm::vec3 maxB) {
		glm::vec3 out(0.0f, 0.0f, 0.0f);

		

		return out;
	}
}

#endif